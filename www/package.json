{
  "name": "@keymapkit/www",
  "version": "1.0.0",
  "private": true,
  "type": "commonjs",
  "license": "MIT",
  "scripts": {
    "build:dev": "BUILD_DRAFTS=true eleventy",
    "build:prod": "eleventy",
    "serve:dev": "BUILD_DRAFTS=true eleventy --serve --port ${ELEVENTYPORT:-8081}",
    "serve:prod": "eleventy --serve --port ${ELEVENTYPORT:-8081}"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.1.1",
    "@11ty/eleventy-navigation": "^1.0.4",
    "@11ty/eleventy-plugin-syntaxhighlight": "^5.0.1"
  }
}
