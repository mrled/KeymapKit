---
title: KeymapKit
---

{% macro renderNav(items) %}
  <ul>
    {% for item in items %}
      <li>
        <a href="{{ item.url | url }}">{{ item.title }}</a>
        {% if item.children %}
          {{ renderNav(item.children) }}
        {% endif %}
      </li>
    {% endfor %}
  </ul>
{% endmacro %}

<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="/KeymapKit/style.css" />
    <link rel="icon" type="image/svg+xml" href="/KeymapKit/cmd-c05621.svg" />
    <link href="/KeymapKit/prism-one-dark.css" rel="stylesheet"/>

    <script type="importmap">
      {
        "imports": {
          "@keymapkit/ui": "/KeymapKit/keymapkit/keymapkit.js",
          "@keymapkit/keyboard.ergodox": "/KeymapKit/keymapkit/keyboard.ergodox.js",
          "@keymapkit/keyboard.planck48": "/KeymapKit/keymapkit/keyboard.planck48.js",
          "@keymapkit/examples": "/KeymapKit/keymapkit/examples.js"
        }
      }
    </script>

    <script
      src="https://cdn.usefathom.com/script.js"
      data-site="HDMUSVII"
      defer
    ></script>
    <script src="/KeymapKit/common.js" defer></script>
    <title>
      {% if eleventyNavigation and eleventyNavigation.title %}{{
        eleventyNavigation.title
      }}{% else %}{{ title }}{% endif %}
    </title>
  </head>
  <body>
    <button class="menu-toggle" aria-label="Toggle menu" aria-expanded="false">
      ☰
    </button>
    <nav id="sidebar">
      <h1><a href="/KeymapKit/">KeymapKit</a></h1>
      {{ renderNav(collections.all | eleventyNavigation) }}
    </nav>
    <main id="content">
      <h1>{{ title | safe }}</h1>
      {{ content | safe }}
    </main>
  </body>
</html>

<!-- -*- mode: html -*- -->
